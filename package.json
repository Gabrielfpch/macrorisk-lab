{
  "name": "macrorisk-lab",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Laboratorio cuantitativo de portfolio construction, correlation, risk decomposition, stress testing y Monte Carlo.",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check src/finance.js"
  },
  "engines": {
    "node": ">=20"
  }
}
